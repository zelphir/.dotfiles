#!/usr/bin/env bash

export PATH=$HOME/.n/bin:$PATH

apps=(
  create-react-app
  create-react-native-app
  gqlint
  import-js
  livedown
  neovim
  prettier
  react-native-cli
  serve
)

yarn global add ${apps[@]} --ignore-engines
